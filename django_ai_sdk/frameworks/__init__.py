"""
Framework-specific bindings for agent orchestration.

Each subdirectory (haystack, etc) contains adapters that integrate the SDK's core
capabilities with specific orchestration frameworks. The core SDK remains
framework-agnostic; these are optional extensions.
"""
